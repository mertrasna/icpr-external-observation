# Synthetic example

The two files in `snapshots/` use IANA documentation address space and invented
location labels. They exist only to exercise additions and label changes; they
are not Apple catalogue observations.

Run the example from the repository root with:

```bash
make setup
make demo-egress
```

Generated files appear in `results/egress-catalogue/generated/demo/`.
